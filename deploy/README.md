# Deployment

Planned:

- local development Compose
- production Compose or supervised services
- Caddy static H5 and API reverse proxy
- explicit migrations
- backup and restore scripts
- health checks

Docker and Caddy are not available on the current local command path, so no unverified deployment configuration is claimed as runnable.

